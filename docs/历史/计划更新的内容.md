# 计划更新的内容（历史归档）

本文件是 `docs/更新计划/计划更新的内容.md` 的历史归档，记录已发布版本的需求完成情况。

> **注意**：本文档为本地规划草稿的历史快照，已加入 `.gitignore`，不进入 Git 跟踪。

---

## 已发布版本归档

### v0.9.0 — 2026-06-18

- [x] 总结近期提交并发布 v0.9.0
- [x] 修复 CLI 分页帮助、`jcvi` 子命令
- [x] 局部共线性目标基因高亮
- [x] 自动目录输入混用模式
- [x] 文档刷新与 `.codex/` 忽略

### v0.9.1 — 2026-06-18

- [x] 细致的 `run.log`（需求 2）
  - 每个子任务（发现输入、预处理、BLAST、JCVI、绘图、汇总）记录开始/结束/耗时/状态
  - 统一日志格式，支持 `--verbose` 与 `--log-level`
  - 失败时输出最后执行的子任务上下文

### v0.9.2 — 2026-06-18

- [x] 外部配置与智然体插件（需求 3、6）
  - 恢复 `analyze run <request.json>` 入口，复用 `AnalysisRequest` 模型与调度器
  - 提供 JSON schema 与示例
  - 智然体插件从直接调用 `analyze mcscan` 改为写入 `genomelens_request.json` 并调用 `analyze run`
  - 智然体插件仅暴露一站式 JCVI 出图流程，输出稳定插件契约

### v0.9.3 — 2026-06-18

- [x] JCVI 子任务命令接口（需求 4）
  - 将引擎侧子任务（如 `graphics_synteny`、`graphics_dotplot`、`local_synteny`）映射为 CLI 子命令
  - 支持 `analyze mcscan jcvi <subtask> <input> <output>` 形式的自动流程
  - 子命令本身决定 workflow，避免与 `--jcvi-workflow` 参数冲突
  - 保持现有 `analyze mcscan jcvi` 一站式入口不变

### v0.9.4 — 2026-06-18

- [x] 多物种局部共线性图与出图优化（需求 1、5）
  - ≥3 物种时聚合参考物种与所有目标物种的局部共线性结果，输出 `multi_species_local.*` 汇总图
  - 出图自动优化开关（默认关闭）：最终图片尺寸、轨道链接重写、`blocks` 跨染色体切除
  - 每项优化独立参数控制，可在 `jcvi.config.json` 或 CLI 中配置

### v0.9.5 — 2026-06-18

- [x] 控制台静默（需求 7 部分）
  - `ConciseConsoleFilter` 让控制台只输出 `Starting GenomeLens` 初始句段与警告/错误
  - 完整日志继续写入 `run.log`
  - 保留 `--verbose` / `--log-level` 供需要完整控制台日志的调试场景
- [x] JCVI 出图尺寸取整
  - `plot_optimization.suggest_figsize` 返回整数 `WxH`，适配 JCVI figsize 格式要求
- [x] Pyright 类型修复
  - `manifest_loader.py` 使用 `_int` 辅助解析 `schema_version`
  - `local_synteny_aggregate.py` 对 `artifacts.get("figures")` 使用 `cast(list[Any], ...)`
  - 在 `catalog_ortholog.py`、`graphics_dotplot.py`、`graphics_karyotype.py`、`local_synteny.py`、`mcscan_pairwise.py` 增加 `query`/`subject` 非空守卫

---

## 原始编号说明（归档）

| 编号 | 需求 | 类型 | 优先级 | 依赖 | 状态 |
|------|------|------|--------|------|------|
| 0 | 总结近期提交并发布 v0.9.0 | 发布 | 高 | 无 | 已完成 |
| 1 | ≥3 物种时输出多物种局部共线性图 | 功能 | 中 | 局部共线性、pairwise 编排 | 已完成 |
| 2 | 完善细致的 `run.log`，每项子任务都记录日志 | 基础 | 高 | 无 | 已完成 |
| 3 | 恢复 `analyze run <request.json>` 外部配置自定义工作流 | 功能 | 高 | 请求模型、调度器 | 已完成 |
| 4 | JCVI 各子任务设置命令接口，兼容 `analyze mcscan jcvi <subtask>` | 功能 | 中 | `jcvi` 子命令结构 | 已完成 |
| 5 | 一站式流程增加出图自动优化（大小、轨道链接、跨染色体 blocks） | 功能 | 中 | graphics/karyotype 工作流 | 已完成 |
| 6 | 更新智然体插件入口，仅对接一站式 JCVI 出图流程 | 集成 | 中 | 插件架构、JCVI 出图流程 | 已完成 |
| 7 | CLI 运行时用进度条/状态行替代大量日志输出 | 体验 | 中 | run.log 稳定、状态机 | 部分完成 |
| 8 | 使用 Tauri 制作 GUI | 功能 | 低 | CLI 稳定、跨平台设计 | 未开始 |
| 9 | 完成 mac 端及 mac GUI 预览版本 | 平台 | 低 | 引擎工具链 mac 移植、GUI | 未开始 |

---

## 历史版本分布快照

### v0.9.x — 补齐 v1.0.0 核心能力

主题：在 v1.0.0 正式发布前，以 0.9.x patch 形式补齐核心流程能力。

- [x] **v0.9.1：细致的 `run.log`（2）**
- [x] **v0.9.2：外部配置与智然体插件（3、6）**
- [x] **v0.9.3：JCVI 子任务命令接口（4）**
- [x] **v0.9.4：多物种局部共线性图与出图优化（1、5）**
- [x] **v0.9.5 / v0.9.x：CLI 体验打磨（7）**
  - [x] 控制台默认静默，只保留初始启动句段与错误信息
  - [ ] 用单行进度条或状态行展示当前步骤、物种对、已完成比例
  - [ ] 失败时给出简洁错误摘要，并提示查看 `run.log` 获取完整上下文
  - [x] 保留 `--verbose` / `--log-level` 供需要完整控制台日志的调试场景

### v1.0.0 — 首个稳定版

主题：核心功能全部稳定，对外正式发布。

- 双物种真实 JCVI 端到端共线性分析
- 多物种 all-vs-all pairwise 编排与全局核型总图
- 多物种以目标基因为中心的局部共线性分析
- 配置文件驱动、Windows 优先的 CLI 与打包支持
- 细致的 `run.log`
- `analyze run <request.json>` 外部配置入口
- 智然体插件入口
- JCVI 子任务 CLI
- 多物种局部共线性图
- 出图自动优化

### v1.1.0 — GUI

主题：提供图形界面，降低非命令行用户门槛。

- [ ] **7. 使用 Tauri 制作 GUI**
  - 基于 Tauri + Web 前端构建跨平台桌面 GUI
  - 第一阶段覆盖：输入目录选择、参数配置、一键运行、结果浏览
  - GUI 底层复用现有 CLI/Python 引擎，不重复实现核心逻辑

### v1.2.0 / `mac` 分支 — 跨平台

主题：将 Windows 优先的实现扩展到 macOS。

- [ ] **8. mac 端与 mac GUI 预览版本**
  - 引擎工具链（BLAST+、JCVI、ImageMagick）macOS 移植与探测
  - 打包脚本适配 macOS（.app / .dmg）
  - 在 `mac` 分支发布预览版本，收集反馈后再合并主线

---

## 历史多人协作分工（v0.9.x）

| 负责人 | 分支 | 负责版本 | 需求 | 关键文件/模块 | 前置条件 | 可并行度 |
|--------|------|----------|------|---------------|----------|----------|
| A | `feature/run-log` | v0.9.1 | 2. run.log | `app/controller/runners/*`、`app/controller/state_machine.py`、新增 `core/logging.py` | 无 | 高 |
| B | `feature/analyze-run-json` | v0.9.2 | 3. analyze run JSON | `cli/commands/analyze.py`、`analysis/normalization/request_assembler.py`、`core/models.py` | 无 | 高 |
| C | `feature/haiant-plugin-entry` | v0.9.2 | 6. 智然体插件入口 | `integrations/haiant_plugin/`、`analysis/methods/mcscan_plugin.py` | v0.9.2 的 `analyze run` 接口或预先约定的 `AnalysisRequest` schema | 中 |
| D | `feature/jcvi-subtask-cli` | v0.9.3 | 4. JCVI 子任务 CLI | `cli/commands/analyze.py`、`cli/mcscan_help.py`、`analysis/methods/mcscan_provider.py` | 无 | 中 |
| E | `feature/multi-species-local-synteny` | v0.9.4 | 1. 多物种局部共线性图 | `engines/jcvi/src/jcvi_genomelens/workflows/local_synteny.py`、`graphics/` | v0.9.3 推荐 | 中 |
| F | `feature/plot-auto-optimization` | v0.9.4 | 5. 出图自动优化 | `engines/jcvi/src/jcvi/graphics/`、`engines/jcvi/src/jcvi_genomelens/workflows/` | 无 | 中 |
| G | `feature/cli-progress-bar` | v0.9.5 / v0.9.x | 7. CLI 进度条/状态行 | `data/logging/log_setup.py`、`app/controller/state_machine.py`、CLI 输出层 | v0.9.1 的 run.log | 中 |

---

## 历史代码审查重点

- run.log：是否覆盖所有子任务，日志格式是否统一。
- analyze run JSON：JSON schema 是否与 CLI 参数等价，错误提示是否清晰。
- 智然体插件：是否按 `analyze run <request.json>` 调用，插件契约是否稳定，是否只依赖公开接口。
- JCVI 子任务 CLI：是否破坏现有 `analyze mcscan jcvi` 默认行为。
- 多物种局部共线性图：是否复用现有 `local_synteny`，是否引入新的 ID 冲突。
- 出图优化：每项优化是否独立开关，默认是否关闭。
- CLI 进度条/状态行：是否不破坏 `--verbose` / `--log-level` 调试输出，失败提示是否清晰指向 `run.log`。

---

## 历史风险与注意事项

- **5 的优化开关**：跨染色体切除若默认开启，可能改变老用户输出；必须显式参数控制。
- **4 的子任务接口**：需避免与现有 `--jcvi-workflow` 参数语义冲突，建议子命令优先、参数回退。
- **8 的 macOS 移植**：工具链路径、动态库、字体渲染与 Windows 差异较大，建议单独 `mac` 分支长期维护。
- **并行冲突热点**：`cli/commands/analyze.py`、`core/models.py`、`app/controller/runners/_shared.py` 是高频冲突区，建议改动前在协作群中同步。

---

## 历史多人协作分工（v0.9.x）

| 负责人 | 分支 | 负责版本 | 需求 | 关键文件/模块 | 前置条件 | 可并行度 |
|--------|------|----------|------|---------------|----------|----------|
| A | `feature/run-log` | v0.9.1 | 2. run.log | `app/controller/runners/*`、`app/controller/state_machine.py`、新增 `core/logging.py` | 无 | 高 |
| B | `feature/analyze-run-json` | v0.9.2 | 3. analyze run JSON | `cli/commands/analyze.py`、`analysis/normalization/request_assembler.py`、`core/models.py` | 无 | 高 |
| C | `feature/haiant-plugin-entry` | v0.9.2 | 6. 智然体插件入口 | `integrations/haiant_plugin/`、`analysis/methods/mcscan_plugin.py` | v0.9.2 的 `analyze run` 接口或预先约定的 `AnalysisRequest` schema | 中 |
| D | `feature/jcvi-subtask-cli` | v0.9.3 | 4. JCVI 子任务 CLI | `cli/commands/analyze.py`、`cli/mcscan_help.py`、`analysis/methods/mcscan_provider.py` | 无 | 中 |
| E | `feature/multi-species-local-synteny` | v0.9.4 | 1. 多物种局部共线性图 | `engines/jcvi/src/jcvi_genomelens/workflows/local_synteny.py`、`graphics/` | v0.9.3 推荐 | 中 |
| F | `feature/plot-auto-optimization` | v0.9.4 | 5. 出图自动优化 | `engines/jcvi/src/jcvi/graphics/`、`engines/jcvi/src/jcvi_genomelens/workflows/` | 无 | 中 |

### 并行原则

1. **v0.9.1（run.log）建议先开第一个 PR**：
   - run.log 会贯穿所有 runner，先合并可以为其他人的调试提供便利。
   - 其它分支只需在合并后补一行 `log.task_started/finished` 即可，冲突面小。

2. **v0.9.2 的两项（analyze run JSON + 智然体插件）建议串行合并，但可并行开发**：
   - analyze run JSON 改动集中在 platform shell 侧，定义 `AnalysisRequest` schema 与 `analyze run <request.json>` CLI。
   - 智然体插件改动集中在 `integrations/` 侧，目前插件已经会写 `genomelens_request.json`，只是把 CLI 调用从 `analyze mcscan` 改成 `analyze run`。
   - **合并顺序**：先合并 `analyze run JSON`，再合并智然体插件。

3. **v0.9.3（JCVI 子任务 CLI）可与 v0.9.2 并行启动**：
   - 只要不动 `analyze mcscan jcvi` 一站式入口的默认行为，就不会影响 B/C。

4. **v0.9.4 的两项建议等 v0.9.3 合并后再大规模开发**：
   - 多物种局部共线性图和出图优化都可能在 CLI 层增加新子命令或参数。
   - 等 v0.9.3 把子命令框架确定后，再基于该框架扩展。

### 集成顺序

```text
main ───────────────────────────────────────────►
      \           \           \           \
       feature/run-log ─────────────►  (v0.9.1)
        \           \
         feature/analyze-run-json ──►  (v0.9.2)
          \
           feature/haiant-plugin-entry ──►  (v0.9.2)
            \
             feature/jcvi-subtask-cli ──►  (v0.9.3)
              \
               feature/multi-species-local-synteny ──►  (v0.9.4)
                \
                 feature/plot-auto-optimization ──►  (v0.9.4)
```
